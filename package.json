{
  "name": "redux-adapter",
  "version": "0.0.1-0",
  "description": "Less and better code to write redux.",
  "repository": {
    "url": "https://github.com/qinyuanbin/redux-adapter.git",
    "type": "github"
  },
  "main": "dist/index.js",
  "scripts": {
    "release": "npm run transpile && standard-version && git push --follow-tags origin master && npm publish",
    "prerelease": "npm run transpile && standard-version --prerelease && git push --follow-tags origin master && npm publish --tag prerelease",
    "transpile": "babel src -d dist --copy-files"
  },
  "keywords": [
    "redux",
    "utils"
  ],
  "author": "qinyuanbin",
  "license": "MIT",
  "peerDependencies": {
    "prop-types": "^15.6.2",
    "react": "^16.4.1",
    "redux": "^4.0.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^7.1.5",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "webpack": "^4.16.2",
    "webpack-cli": "^3.1.0"
  }
}
